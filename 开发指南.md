# PaperMemory 开发指南

## 变更日志

### v1.1.0 - Abstract 字段支持 (2025-12-28)

#### 新增功能
- 为论文数据添加 `abstract` 字段
- 支持从 arXiv API 自动提取论文摘要
- Notion 双向同步支持（Rich Text 类型，最多 2000 字符）

#### 数据迁移
- **数据版本**：10000 → 10100
- 所有现有论文自动添加空的 `abstract` 字段
- 迁移日志：`(m10100) add abstract field with default empty string`

#### 技术实现

**1. 数据模型变更 ([src/shared/js/utils/data.js](src/shared/js/utils/data.js))**

在 `validatePaper()` 函数中添加了 abstract 字段定义：
```javascript
abstract: {
    type: "string",
    desc: "the paper's abstract",
    default: (p) => "",
    validation: (p) => {
        if (typeof p !== "string") {
            return `Invalid abstract type: expected string, got ${typeof p}`;
        }
    },
}
```

**2. arXiv 解析器增强 ([src/shared/js/utils/parsers.js](src/shared/js/utils/parsers.js))**

在 `makeArxivPaper()` 函数中添加了 abstract 提取逻辑：
- 从 arXiv API 返回的 XML 中的 `<summary>` 标签提取摘要
- 自动清理多余的空白字符和换行符
- 返回值中包含 abstract 字段

```javascript
// 提取 abstract（从 <summary> 标签）
let abstract = doc.querySelector("entry summary");
abstract = abstract?.textContent || abstract?.innerText || "";
// 清理多余的空白字符和换行符
abstract = abstract.trim().replace(/\s+/g, " ");
```

**3. Notion 集成 ([src/shared/js/utils/notion.js](src/shared/js/utils/notion.js))**

添加了 Abstract 字段的双向映射：

- **本地 → Notion** (`paperToNotionProperties()`)：
  ```javascript
  "Abstract": {
      rich_text: [{ text: { content: truncate(paper.abstract || "", 2000) } }]
  }
  ```

- **Notion → 本地** (`notionPropertiesToPaper()`)：
  ```javascript
  abstract: getText(props["Abstract"])
  ```

#### 支持的论文来源
- ✅ **arXiv**：从 API 自动提取 abstract
- ❌ **其他来源**：暂不支持（abstract 字段为空字符串）

#### Notion 配置说明

用户需要在 Notion 数据库中手动添加 "Abstract" 字段：
- **字段名称**：Abstract
- **字段类型**：Rich Text
- **说明**：如果 Notion 数据库中没有此字段，同步时会自动忽略（不会报错）

#### 边界情况处理

1. **Abstract 过长**：
   - 本地存储完整 abstract（无长度限制）
   - Notion 同步时自动截断到 2000 字符

2. **Abstract 为空**：
   - arXiv 论文如果没有 abstract，设置为空字符串
   - 其他来源的论文 abstract 默认为空字符串

3. **特殊字符**：
   - 保持原始内容（包括 LaTeX 公式）
   - 自动清理多余的空白字符

#### 测试建议

**开发测试**：
1. 运行 `npx gulp watch` 启动开发模式
2. 在浏览器中加载扩展
3. 访问 arXiv 论文页面（如 https://arxiv.org/abs/1703.10593）
4. 打开 popup，检查论文数据
5. 使用开发者工具查看 `chrome.storage.local`，验证 abstract 字段

**Notion 同步测试**：
1. 在 Notion 数据库中添加 "Abstract" 字段（Rich Text 类型）
2. 同步论文到 Notion，验证 abstract 显示正确
3. 在 Notion 中修改 abstract，同步回本地，验证数据一致性

#### 未来扩展方向

- 支持更多来源的 abstract（OpenReview、Semantic Scholar、PubMed 等）
- 在 Popup UI 中显示 abstract
- Abstract 搜索功能
- Abstract 翻译功能

---

## 数据结构

### Paper 对象完整字段列表

| 字段 | 类型 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `id` | string | ✅ | - | 唯一的 PaperMemory ID |
| `title` | string | ✅ | - | 论文标题 |
| `author` | string | ✅ | - | 作者列表（用 ` and ` 分隔） |
| `pdfLink` | string | ✅ | - | PDF 链接 |
| `source` | string | ✅ | - | 论文来源 |
| `abstract` | string | ❌ | `""` | 论文摘要（v1.1.0+） |
| `year` | string | ❌ | - | 发表年份（4位数字） |
| `venue` | string | ❌ | `""` | 发表场所 |
| `bibtex` | string | ❌ | `""` | BibTeX 引用 |
| `doi` | string | ❌ | 从 bibtex 提取 | DOI |
| `key` | string | ❌ | `defaultKey_${id}` | BibTeX key |
| `tags` | array | ❌ | `[]` | 用户标签 |
| `note` | string | ❌ | `""` | 用户笔记 |
| `codeLink` | string | ❌ | `""` | 代码链接 |
| `code` | object | ❌ | `{}` | PapersWithCode API 数据 |
| `favorite` | boolean | ❌ | `false` | 是否收藏 |
| `favoriteDate` | string | ❌ | `""` | 收藏日期 |
| `count` | number | ❌ | `1` | 访问次数 |
| `addDate` | string | ❌ | 当前时间 | 添加日期 |
| `lastOpenDate` | string | ❌ | 当前时间 | 最后打开日期 |
| `md` | string | ❌ | `[${title}](${pdfLink})` | Markdown 格式 |
| `extras` | object | ❌ | - | 额外信息（可选） |

---

## 开发流程

### 添加新的论文字段

如果需要为 paper 对象添加新字段，请按以下步骤操作：

1. **在 [src/shared/js/utils/data.js](src/shared/js/utils/data.js) 中添加字段验证**：
   - 在 `validatePaper()` 的 `expectedKeys` 对象中添加字段定义
   - 指定类型、描述、默认值和验证函数

2. **添加数据迁移逻辑**：
   - 更新 `latestDataVersion` 版本号
   - 在 `migrateData()` 中添加迁移逻辑
   - 为现有论文设置默认值

3. **在解析器中提取字段**（如果需要）：
   - 在 [src/shared/js/utils/parsers.js](src/shared/js/utils/parsers.js) 中修改相应的 `make*Paper()` 函数
   - 从数据源提取字段值

4. **添加 Notion 同步支持**（如果需要）：
   - 在 [src/shared/js/utils/notion.js](src/shared/js/utils/notion.js) 中添加字段映射
   - 修改 `paperToNotionProperties()` 和 `notionPropertiesToPaper()` 函数

5. **更新测试数据**：
   - 在测试文件中添加新字段

6. **更新文档**：
   - 在本文档中记录变更

### 构建和测试

**开发模式**：
```bash
npx gulp watch
```

**生产构建**：
```bash
npx gulp build
```

**运行测试**：
```bash
npm test
```

---

## 注意事项

1. **必须运行 gulp**：HTML 文件使用预处理，修改后需要 gulp 编译
2. **内容脚本修改**：需要重新加载扩展并刷新页面
3. **数据备份**：`migrateData()` 会自动备份数据到 `papersBackup`
4. **向后兼容**：新字段应该有默认值，确保旧版本不会报错

---

## 2026-02-21: 配置+数据整机迁移包

### 功能概览
新增“整机迁移包”能力：在 Options > Data Management 中支持导出和导入单文件 `PaperMemory-migration-*.json`。

迁移包包含：
- 论文数据：`papers`（含 `__dataVersion`）
- 本地配置：`prefs`、`pwcPrefs`、`autoTags`、`ignoreSources` 等
- 敏感凭证：`syncPAT`、`notionToken`、`aiApiKey` 等

### 关键实现
1. `src/shared/js/utils/config.js`
- 新增 `global.migrationConfigKeys`，集中定义迁移包配置键白名单。

2. `src/shared/js/utils/data.js`
- 新增 `migrationPackageSchemaVersion`
- 新增 `normalizeMigrationConfig(config)`：缺失键统一补 `null`
- 新增 `buildMigrationPackage()`：构建完整迁移包
- 新增 `validateMigrationPackage(payload)`：校验迁移包 schema
- 新增 `prepareMigrationImportData(payload)`：导入前校验 + 数据兼容迁移预处理

3. `src/options/options.html`
- 在 Data Management 区新增：
  - Export full migration package
  - Import migration package（覆盖导入）
- 增加明文敏感信息风险提示。

4. `src/options/options.js`
- 新增迁移包导出处理器。
- 新增迁移包导入处理器（文件选择、JSON 解析、schema 校验、确认覆盖、写入 storage）。
- 导入会先清理 `migrationConfigKeys` 再批量写入，避免保留旧设备残留配置。

### 导入行为说明
- 导入策略：覆盖本地 `papers` 和迁移配置键。
- 版本策略：先走现有迁移逻辑（`prepareOverwriteData`/`migrateData`），失败则阻断导入。
- 同步策略：导入流程不主动触发远端 push/pull，同步仍按用户后续操作触发。

### 测试
新增测试文件：`test/test-migration-package.js`
- `normalizeMigrationConfig`：缺省键补全、保留 falsey 值
- `validateMigrationPackage`：合法包、新 schema 拒绝、缺失字段拒绝、旧数据版本允许
- `buildMigrationPackage`：导出结构与配置快照完整性

运行记录：
- `./node_modules/.bin/mocha test/test-migration-package.js` 通过（7 passing）
- `npx gulp build` 通过

已知说明：
- 当前仓库的 `npm run test-no-browser` 在本分支存在既有环境问题（`fetchArxivXML is not defined`），与本次改动无直接关系。

---

## 2026-02-22: Supabase 主库自动同步（方案脑暴）

### 本次变更
- 新增方案文档：`docs/brainstorms/2026-02-22-supabase-primary-sync-brainstorm.md`
- 当前仅完成需求澄清与方案收敛，未开始代码实现。

### 已确认决策
- Supabase 作为主库（远端真相源）
- 冲突时远端覆盖本地
- 自动拉取采用固定间隔，默认 60 分钟
- 首期只同步 `papers`
- 数据模型采用结构化表（每篇 paper 一行）
- 使用 `syncKey` 作为命名空间隔离
- 写回策略为“手动上传 + 可选自动写回”
- 拉取失败时弹窗提示并要求处理
- 本地删除不保留优先级，远端可回流

### 下一步
- 进入 `workflows-plan` 输出实现计划，再按计划开发与测试。

---

## 2026-02-22: Supabase 主库同步计划（workflows-plan）

### 本次变更
- 新增计划文档：`docs/plans/2026-02-22-feat-supabase-primary-papers-sync-plan.md`
- 计划来源：`docs/brainstorms/2026-02-22-supabase-primary-sync-brainstorm.md`

### 计划核心
- Supabase 作为主库，远端覆盖本地
- 默认 60 分钟定时拉取
- 首期仅同步 `papers`
- 支持手动全量上传 + 可选自动写回
- 使用 `syncKey` 命名空间，不引入 Supabase Auth（首期）

### 备注
- 当前阶段仅完成研究与计划编制，尚未进入代码实现阶段。

---

## 2026-02-22: Supabase 主库同步功能实现

### 功能概览
已实现 Supabase 主库同步（首期仅 `papers`）：
- Supabase 作为主库，支持手动 Push / Pull
- Pull 采用远端覆盖本地
- 支持定时自动 Pull（默认 60 分钟）
- 支持可选自动 Push（保存论文后写回单条）
- 定时拉取失败会自动禁用 auto-pull 并记录阻断错误，用户在 Options 中处理后再开启

### 关键代码变更
1. `src/shared/js/utils/config.js`
- 新增 Supabase 配置键：
  - `supabaseUrl`
  - `supabaseAnonKey`
  - `supabaseSyncKey`
  - `supabaseSyncState`
  - `supabaseAutoPullEnabled`
  - `supabaseSyncInterval`
  - `supabaseAutoPushEnabled`
- 以上键已纳入 `global.migrationConfigKeys`，支持整机迁移包导入导出。

2. `src/shared/js/utils/supabase.js`（新增）
- 实现 Supabase REST 同步工具：
  - 配置校验（含 `syncKey` 长度）
  - 连接测试
  - 批量 upsert（按批次）
  - 分页拉取并重建 `papers`

3. `src/shared/js/utils/sync.js`
- 新增 Supabase wrapper：
  - `pushToSupabase`
  - `shouldSyncSupabase`
  - `shouldAutoPushSupabase`
  - `initSupabaseSync`
  - `syncAllToSupabase`
  - `pullAllFromSupabase`

4. `src/shared/js/utils/paper.js`
- 论文保存后新增 Supabase 自动写回分支：
  - 仅在 `supabaseSyncState=true` 且 `supabaseAutoPushEnabled=true` 时触发。

5. `src/background/background.js`
- 新增 Supabase 编排：
  - 单条写回 `pushSupabaseSyncPaper`
  - 全量写回 `syncAllSupabasePapers`
  - 全量拉取并覆盖 `syncAllFromSupabase`
  - 自动拉取调度 `setupSupabaseAutoPull`
- 新增 runtime message 类型：
  - `testSupabaseConnection`
  - `testStoredSupabaseConnection`
  - `writeSupabaseSync`
  - `syncAllSupabasePapers`
  - `syncAllPapersFromSupabase`
  - `setupSupabaseAutoPull`
- 新增 alarm：`supabaseAutoPull`

6. `src/options/options.html` + `src/options/options.js`
- 新增 Supabase 配置与控制区：
  - 凭证保存、连接测试
  - 手动 Push/Pull
  - auto-pull 开关与间隔
  - auto-push 开关
- Pull 操作带明确覆盖确认。
- 检测到阻断错误时弹窗提示并要求用户修复后重新开启。

7. `gulpfile.mjs`
- `utilsJS` 构建链路加入 `src/shared/js/utils/supabase.js`。

8. 文档
- `docs/features.md`：新增 Supabase 主库同步说明。
- `docs/configuration.md`：新增 Supabase 配置说明。

### 测试
新增测试：
- `test/test-supabase-sync.js`
- `test/pm-supabase-sync.spec.js`（需环境变量，默认可跳过）

本地运行记录：
- `./node_modules/.bin/mocha test/test-supabase-sync.js test/test-migration-package.js` 通过（11 passing）
- `npx gulp build` 通过
- `npx playwright test test/pm-supabase-sync.spec.js` 执行成功（1 skipped，因未设置 Supabase 环境变量）

---

## 2026-02-22: Supabase 文案调整（移除 RLS 字样）

### 本次变更
- `src/options/options.html`
  - Supabase Setup Instructions 中将“Enable RLS...”改为“Configure table access policies...”。
- `src/options/options.js`
  - 定时拉取失败提示从“credentials/RLS”改为“credentials/access settings”。

### 说明
- 仅调整用户可见文案，未修改同步逻辑。

---

## 2026-02-22: Supabase 同步问题修复（review findings）

### 修复内容
1. `src/shared/js/utils/supabase.js`
- URL 校验从仅允许 `https://` 调整为允许 `http://` 与 `https://`，支持本地/自建 Supabase 网关场景。

2. `src/options/options.js`
- 修正手动 Pull 失败提示文案，移除“auto pull 会被阻断”的误导性描述，保持与实际行为一致。

3. `test/test-supabase-sync.js`
- 补充 `http://localhost:54321` 作为合法 URL 的校验用例。

### 验证
- `node --check src/shared/js/utils/supabase.js` 通过
- `node --check src/options/options.js` 通过
- `./node_modules/.bin/mocha test/test-supabase-sync.js` 通过（4 passing）
